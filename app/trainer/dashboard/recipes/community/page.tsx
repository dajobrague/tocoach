import { CommunityGallery } from "@/features/trainer/recipes/community-gallery";

// The shared recipe gallery: recipes trainers across TopCoach chose to
// publish, importable as standalone copies (copy-install model).
export default function CommunityRecipesPage() {
  return <CommunityGallery />;
}
