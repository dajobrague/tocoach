// Airtable client configuration
// Server-only client for client data management

// Implementation to be added when Airtable is configured

export interface AirtableConfig {
  apiKey: string;
  baseId: string;
}

// Placeholder export to prevent TypeScript errors
export const airtableClient = null;
