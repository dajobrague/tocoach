// Supabase client configuration
// Server-only client for trainer authentication

// Implementation to be added when Supabase is configured

export interface SupabaseConfig {
  url: string;
  anonKey: string;
  serviceRoleKey?: string;
}

// Placeholder export to prevent TypeScript errors
export const supabaseClient = null;
